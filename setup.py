from setuptools import setup, find_packages

setup(
    name="workable",
    version="0.1.0",
    packages=find_packages(),
    install_requires=[
        # 依赖项
    ],
    author="Workable Team",
    author_email="example@example.com",
    description="Workable framework",
    keywords="workable",
    python_requires=">=3.7",
) 